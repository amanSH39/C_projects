#include <stdio.h>
#include <conio.h>

int main()
{
    int r;
    int l;
     printf("Enter the radius \n");
     scanf("%d", &r);
      printf("Enter the length \n");
     scanf("%d", &l);
     printf("the area is %f \n", 3.14*r*r*l);
     return 0;
}